// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.

$(document).ready(function(){
	$(function() {
    var BV = new $.BigVideo({
    	container: $(".hero-vid"),
    	controls: false
    });
    var myVid = $('#logo').attr({ src: "<%= asset_path('logo.png') %>" })
    BV.init();
    BV.show('<%= asset_path('HFD_EmergencyPlan_Clip_1.mp4') %>',{ambient:true});
	});

});



